{% trans_default_domain 'sfs_customer' %}
{% extends '@SfsCustomer/admin_layout.html.twig' %}

{% block title %}{{ 'admin_customers.create.meta.title'|trans }}{% endblock %}

{% block breadcrums_content %}
    {{ parent() }}
    <li class="breadcrumb-item"><a href="{{ url('sfs_customer_admin_customers_list') }}">{{ 'admin_customers.breadcrumb'|trans }}</a></li>
    <li class="breadcrumb-item active" aria-current="page">{{ 'admin_customers.create.breadcrumb'|trans }}</li>
{% endblock breadcrums_content %}

{% block content %}
    <div class="d-flex align-items-center justify-content-between mb-4">
        <h1 class="h3">{{ 'admin_customers.create.title'|trans }}</h1>
    </div>
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <div class="d-flex justify-content-end align-items-center">
        <a href="{{ url('sfs_customer_admin_customers_list') }}" class="btn btn-outline-secondary">{{ 'admin_customers.create.actions.cancel.button'|trans }}</a>
        <input type="submit" value="{{ 'admin_customers.create.actions.create.button'|trans }}" class="btn btn-primary ml-3" />
    </div>
    {{ form_end(form) }}
{% endblock content %}